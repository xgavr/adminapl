<?php
$this->headTitle('Файлы с прайсами к загрузке');

$this->mainMenu()->setActiveItemId('rb');

$this->pageBreadcrumbs()->setItems([
            'Главная'=>$this->url('home'),
            'Товары'=>$this->url('goods'),
            'Очередь файлов с картинками' => $this->url('image')
            ]);  

?>

<h1>Очередь файлов с картинками</h1>
<div class="row">
    <div class="col-sm-9">
        <div class="panel-group">
            <?php if (count($files)): ?>
                <?php foreach($files as $fileInfo):?>
                    <div class="thumbnail">                        
                        <a href="<?= '/img/goods/tmp/'.$fileInfo->getFileName() ?>" target="_blank">
                            <img src="<?= '/img/goods/tmp/'.$fileInfo->getFileName() ?>" class="img-thumbnail" alt="<?= $fileInfo->getFileName() ?>">
                        </a>    
                        <div class="caption panel-body small">
                            <div class="pull-left">
                                <?= $fileInfo->getFileName() ?>
                            </div>
                            <div class="pull-right">
                                <button value="/goods/delete-image/" class="btn btn-warning btn-xs this-delete"
                                        title="Удалить">
                                    <span class="glyphicon glyphicon-remove" ></span>
                                </button>                             
                            </div>                                                                            
                        </div>                    
                    </div>
                <?php endforeach; ?>
            <?php endif; ?>
        </div>
    </div>
</div>