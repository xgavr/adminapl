<?php
$this->headTitle($goods->getName());
$this->mainMenu()->setActiveItemId('goods');

$breadcrumbs_items = ['Главная' => $this->url('home')];
if ($this->access('good.manage')){
    $breadcrumbs_items['Товары'] = $this->url('goods');
} else {
    $breadcrumbs_items['Каталог'] = $this->url('shop');    
}
$breadcrumbs_items[$goods->getName()] = $this->url('goods', ['action' => 'view', 'id' => $goods->getId()]);

$this->pageBreadcrumbs()->setItems($breadcrumbs_items);        
?>

<h1>
    <?= $this->escapeHtml($goods->getName()); ?>    
</h1>


<p>    
    <?= $this->escapeHtml($goods->getCode()); ?>
</p>

<p>    
    <?= $this->escapeHtml($goods->getProducer()->getName()); ?>
</p>

<p>    
    <?= $this->escapeHtml($goods->getTax()->getName()); ?>
</p>

<p>    
    <?= $this->escapeHtml($goods->getAvailable()); ?>
</p>

<p>    
    <?= $this->escapeHtml($goods->getDescription()); ?>
</p>

